# `@solana-config/prettier`

Shared [Prettier](https://prettier.io/) configuration for Solana projects.

## Install

```bash
pnpm add -D @solana-config/prettier prettier
```

## Usage

Add a reference to this module from your `package.json`:

```json
{
    "prettier": "@solana-config/prettier"
}
```
